description "{{ _hubot.instance }}"
start on filesystem and started docker
stop on runlevel [!2345]
respawn
script
  cd {{ _hubot.data_directories.base }}/{{ _hubot.data_directories.docker }}
  /usr/local/bin/docker-compose start
end script
